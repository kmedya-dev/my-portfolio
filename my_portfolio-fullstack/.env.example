# .env.example
# This file lists the environment variables required by the project.
# Create a .env file (NOT committed to Git) based on this example and fill in the values.

# Django Superuser Credentials
DJANGO_SUPERUSER_EMAIL=your_email@example.com
DJANGO_SUPERUSER_USERNAME=your_username
DJANGO_SUPERUSER_PASSWORD=your_password

# Database URL for local development (e.g., your Render external URL or a local PostgreSQL)
DEV_DATABASE_URL=postgresql://user:password@host:port/dbname

# Set to True for local development, False for production
DEBUG=False

# Django Secret Key (generate a strong one for production)
SECRET_KEY='your_secret_key_here'

# ⚠️ IMPORTANT: The .env file (containing actual values) must NEVER be committed to Git.
# Add .env to your .gitignore file.